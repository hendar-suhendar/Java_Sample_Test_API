import org.testng.Assert;
import org.testng.annotations.Test;

import static io.restassured.RestAssured.*;
import io.restassured.response.Response;

public class TS_1_GET_dsadsa {
	String baseURL = "https://reqres.in";
	@Test
	void test_01() {
		Response response = get(baseURL+"/api/users?page=2");
		System.out.println(response.getHeader("content-type"));
		System.out.println(response.getTime());
		System.out.println(response.asString());
		System.out.println(response.getStatusCode());
		System.out.println(response.getBody().asPrettyString());
		System.out.println(response.getStatusLine());
		 
		//Verify Equals
		int statusCode = response.getStatusCode();
		Assert.assertEquals(statusCode, 201);
		//verify JSON Schema
		
			/*
			 * given() .when().get()
			 * 
			 * //verify JSON Schema .then().assertThat() .body(JsonSchemaValidator.
			 * matchesJsonSchema(new File("/Users/src/Parameterize/schema.json")));
			 */
		//get("/lotto").then().body("lotto.lottoId", equalTo(5));
	}
	@Test
	void test_02() {
		given().get(baseURL+"/api/users?page=2").then().statusCode(200);
		
	}
	
}
